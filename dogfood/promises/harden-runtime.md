# Promise

- `promise`: The runtime should still create a readable receipt after one successful check.
- `scope`: `runtime/src/tereo`
- `check`: `PYTHONPATH=runtime/src python3 -m tereo --help`
- `proof rule`: Keep only if the command exits cleanly and the command surface stays simple.
- `stop condition`: One kept patch, one discarded patch, or one real blocker
